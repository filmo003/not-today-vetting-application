version: '3'

services:
  dev-web:
    build:
      context: .
      dockerfile: dev.dockerfile
    volumes:
      - '.:/project'
    working_dir: /project/frontend
    entrypoint: npm start
    ports:
      - '0.0.0.0:1337:1337'